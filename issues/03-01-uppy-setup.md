# Task 3.1: Setup Uppy.io instance

## Description
Initialize Uppy.io for handling file uploads.

## Requirements
- Install `@uppy/core`, `@uppy/react`, `@uppy/aws-s3`.
- Configure Uppy instance.
- Prepare the UI container for the uploader.

## Acceptance Criteria
- [ ] Uppy dashboard or drag-drop area is visible.
- [ ] Uppy is initialized without errors.
