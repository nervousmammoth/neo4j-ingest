# Task 5.2: Configure persistent Environment Variables

## Description
Ensure production environment variables are correctly set and persisted.

## Requirements
- `AWS_ACCESS_KEY`, `AWS_SECRET_KEY`, `BUCKET_NAME`, `REGION`.
- Secure storage of secrets.

## Acceptance Criteria
- [ ] App can read env vars in production mode.
